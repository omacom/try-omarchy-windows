# Live audio r20 engineering check: September 23, 2026

## Packaged guest follow-up

An isolated portable install on the AMD Windows laptop used the rebuilt guest
from [guest build 35925099916](https://github.com/omacom/try-omarchy-windows/actions/runs/35925099916)
and the verified r20b runtime. Its combined payload `SHA256SUMS` has SHA-256
`9fba355614b95dc61e118daeb671b2169d5ea1b869031f68e1998a0c061fb5c3`.
The unsigned test launcher has SHA-256
`35b5cb4dbec1d56ed3440f50228c1f2b3d0060ca5b8e9222245424b73cd07391`.
The test installed into `D:\TryOmarchy-Audio-R20-Candidate-20260923` with a
separate `data` folder and left the public installation alone.

- The first boot reached userspace and started the installed
  `omarchy-windows-audio-bridge.service` automatically. Its udev rule gave the
  user access to `/dev/virtio-ports/dev.tryomarchy.audio`. PipeWire listed
  Windows System Default, the Realtek speaker and the AMD microphone.
- Choosing the physical speaker and microphone in Omarchy wrote both stable
  endpoint IDs and friendly names to the candidate's host preference files.
  The live route files initially matched those choices. A guest reboot
  relaunched QEMU; the service restarted automatically and both PipeWire
  defaults returned to the selected physical devices. The host preference
  files still held the same IDs and names, but the live route files reverted
  to `default`. The launcher had reused its startup audio snapshot.
- An updated unsigned launcher (SHA-256
  `c96f73260b6b197c8d86ca07a393ca5ffc03065ccf6c869c677b863087fde4e7`)
  reloads saved audio choices and stable endpoint IDs before each VM start.
  On the same isolated disk, switching both guest choices to Windows System
  Default and rebooting left both live route files at `default`. Switching
  back to the Realtek speaker and AMD microphone and rebooting left both live
  route files on those physical names. The installed service started
  automatically on both boots, and the guest PipeWire defaults matched.
  The launcher and QEMU remained isolated from the public installation.
- A signed-in Windows session probe on the fresh reboot found QEMU's Realtek
  playback session **active before a guest playback stream**. This confirms the
  r20b initial-open idle issue. Patch 0017 defers that open for the next runtime
  build; r20c still needs a physical idle and first-playback check.
- The first signed-in desktop screenshot showed a black QEMU canvas while the
  laptop was asleep. After waking it, a fresh Windows screenshot showed the
  Omarchy bar, wallpaper, and an open terminal in a usable QEMU window. The
  host SSH tunnel dropped during sleep and was recreated with its pinned host
  key; the guest service and physical audio choices remained active.

The r20c runtime from [build 35931045895](https://github.com/omacom/try-omarchy-windows/actions/runs/35931045895)
passed its Windows build, smoke test, and artifact verification. Its portable
ZIP has SHA-256 `748a25068f6e1cc30b2d73f089fdefdc182ec2f3f0653687c13e72230ca6b593`.
The combined r20c guest/runtime manifest has SHA-256
`c125e11481f1b260e390189975d201e8be99c3237e9fecabc72f7d056b2c7df0`.
The same isolated laptop install used the new versioned payload and kept the
r20b runtime for rollback. Its installed QEMU executable had SHA-256
`44e6e56f88fcac5567bfc3995aa5113135efe18b22cd2d0316eb30819c254447`,
matching the verified r20c ZIP.

- In the signed-in Windows session, three samples found no QEMU playback
  session before guest playback. A 15-second guest `pw-play` stream yielded
  three active Realtek playback samples. Three samples after it ended showed
  the session inactive.
- Guest `pw-record` captured 768000 bytes of raw microphone data from the AMD
  device during an eight-second stream. Three signed-in Windows samples showed
  the AMD capture session active during recording and inactive afterward.
  The data was discarded; no human listening check was done.
- After another guest reboot, the installed user audio service was active and
  enabled. PipeWire kept the Realtek speaker and AMD microphone as defaults,
  both encoded live host routes decoded to those device names, and three idle
  Windows samples again found no QEMU playback session.
- A fresh Windows desktop screenshot after that reboot showed the rendered
  Omarchy wallpaper and bar in the QEMU window, with the Windows taskbar below.

That packaged r20c launcher was unsigned because the protected signing
environment accepts only `master`. The signed follow-up below uses the merged
source. Public `v0.2.0` is unchanged.

## Signed r20c candidate on the laptop

After [PR #175](https://github.com/omacom/try-omarchy-windows/pull/175)
merged as `5e66bdbbabbcc87d47987e369aeafebb1ab5c6bb`, the protected
[signing workflow 35934166261](https://github.com/omacom/try-omarchy-windows/actions/runs/35934166261)
built a test launcher with the combined guest/r20c manifest digest
`c125e11481f1b260e390189975d201e8be99c3237e9fecabc72f7d056b2c7df0`
embedded. Its SHA-256 is
`a82ab7da5266bd399c1635ed11c9055af37d9b3e5931815f8eaab4150b56e369`.
Windows reported a valid Authenticode signature from Brandon South, and the
transferred launcher and laptop manifest hashes matched the workflow record.
The runtime flag used the same manifest digest. The isolated portable install
and public `v0.2.0` installation remained separate.

- Switching the existing portable disk to the new guest manifest pin first
  created and verified a 6.5 GB checkpoint, detached the disk from its old
  factory backing image, compared the standalone qcow2 to its raw source, and
  installed the verified guest files. This full safety transition took about
  43 minutes on the laptop. The installed guest receipt records the combined
  manifest digest.
- The signed launcher booted the installed guest with GPU acceleration. A
  Windows screenshot showed the rendered Omarchy desktop. Its audio bridge
  service was active and enabled, the guest PipeWire defaults selected the
  Realtek speaker and AMD microphone, and both live host routes decoded to
  those saved device names. Three Windows samples before playback found no
  QEMU playback session.
- During a 15-second guest `pw-play`, three Windows samples showed active
  Realtek playback. Three samples after playback showed it inactive. Guest
  `pw-record` returned 768000 raw bytes; three Windows samples showed active
  AMD recording during capture and inactive playback. Both sessions were
  inactive in three samples after capture. The recorded data was discarded;
  no human listening test was done.
- A guest reboot relaunched QEMU. After PipeWire initialized, the installed
  service was active and enabled, its selected sink and source were still the
  Realtek and AMD devices, and both host route files retained those names.
  Three idle Windows samples for the new QEMU PID found no playback session.
  A fresh Windows screenshot showed the rendered desktop after the reboot.
  The guest then powered off cleanly; no isolated launcher or QEMU process
  remained.

The signed test launcher is an engineering candidate. No app release, guest
image release, or runtime release was published by this test. Additional
physical endpoints and unplug/reconnect behavior require suitable hardware.

## Guest PipeWire picker follow-up (same laptop)

The unsigned launcher `TryOmarchy-audio-guestpicker.exe` (SHA-256
`9d101f941f228293646cca410da355173a741a4751d802c4d4ae151e43c0d5d7`)
ran with the same isolated r20b runtime and engineering guest. The matching
guest bridge was run as a temporary user service after granting the audio group
access to its new virtio serial port. Patch 0091 includes that udev rule and
the persistent guest service; a rebuilt factory image was not used in this pass.

- Omarchy's PipeWire graph showed `Windows System Default` plus `Speaker
  (Realtek(R) Audio)` as playable sinks, and `Windows System Default` plus
  `Microphone Array (AMD Audio Device)` as recording sources.
- Choosing the physical speaker and microphone in the guest wrote their stable
  Windows endpoint IDs to `audio-endpoints.json`, their SDL names to
  `audio-preferences.json`, and the separate encoded live route files. The
  choices appeared as the guest's PipeWire defaults.
- A 15-second guest `pw-play` stream yielded five interactive Windows session
  samples with QEMU playback **active** on `Speaker (Realtek(R) Audio)`.
  A guest `pw-record` yielded five samples with QEMU recording **active** on
  `Microphone Array (AMD Audio Device)`. No user listening check was requested.
- Choosing `Windows System Default` again returned both host preference files
  to schema-only defaults and both live route files to `default`. The first
  quick pair of selections exposed a catalog echo race. The bridge now captures
  a pending second choice before applying that echo. A second physical boot
  switched output and input rapidly in both directions; after five seconds,
  both guest defaults and both host preference files matched the choices.
- The temporary bridge stopped, its guest files were removed, and the guest
  powered off cleanly. No candidate process remained. The public installation
  and runtime were not replaced.

The host bridge parser, stable-ID filtering, microphone gate and live-route
save passed on the Windows laptop. The patched guest source contract passed
166 tests (one skip). This pass establishes one physical endpoint per direction;
it does not establish two-endpoint switching, hotplug, an automatic Windows
default-device change, or a Windows laptop boot with the rebuilt image.

The final source at `38ec81c` then passed the complete [guest candidate build
and boot smoke](https://github.com/omacom/try-omarchy-windows/actions/runs/35925099916),
including explicit checks for the enabled audio service and its udev rule. The
downloaded artifact's packaged files matched its SHA256SUMS. A disposable
upgrade from the verified public `v0.2.0` factory passed all five boots in
`smoke-guest-upgrade.py`: seed, candidate upgrade, candidate reboot, old-image
boot, and return to candidate. The resulting disk retained revision `33`, the
executable audio bridge, its enabled service link, and the audio-port rule.
This validates delivery to persistent disks; the Windows laptop test above used
a temporary copy of the service instead of this rebuilt image.

This is an isolated candidate check on the AMD Ryzen 5 5625U Windows 11
laptop. Public `v0.2.0` remains on r19. The normal installation and public
runtime were not replaced. The engineering guest is the existing moved
`v0.0.20-preview` test guest, explicitly pinned to its recorded release and
manifest so it did not download a newer image during the audio test.

## Exact artifacts

| Artifact | SHA-256 |
| --- | --- |
| Candidate launcher `TryOmarchy-r20-live-audio-20260923.exe` | `624c5ccc194b5af95c38608f359ff93ad0edda0597641c64bd64e95fbf4619b1` |
| First r20 portable ZIP, rejected for fallback retry behavior | `6a5d306958891c754a60b314e7713cc6bee1d00afa06dc6eefe7cca128bc53ee` |
| First r20 QEMU executable | `e1b3b68978c5cbda963f26c7d444acf9b21d0ce1b59da74ae293b3620e20b8d2` |
| Corrected r20b portable ZIP | `5c853088e34e21cba7783ca8d38e624e40c9629934aaf349143f53014cae3304` |
| Corrected r20b source ZIP | `c82372666c6665cb6bf17b8117077c2471493644376aa6bc1fa9269679b5ed61` |
| Corrected r20b QEMU executable | `a09e658d3b458b834b586e1a269a0193eb6055fc6529d93093372ba8523349f1` |

The [r20b Runtime workflow](https://github.com/omacom/try-omarchy-windows/actions/runs/35880497638)
passed the complete Windows source build, smoke tests and archive verification.
`runtime-build/verify.py` passed again on the downloaded artifact. The staged
QEMU executable came from that verified ZIP and its provenance contains
`0016-live-sdl-audio-routes.patch`. The candidate used WHPX, virgl/Venus and
`-audiodev sdl`, then reached the normal GPU desktop.

## Probe correction

The first checks ran `probe-audio-sessions.exe` through OpenSSH in Windows
session 0. QEMU was in the signed-in desktop's session 1, so those probes could
not enumerate its audio sessions. A supposedly missing session was a **test
error**, not proof of an r20 playback failure. The same incorrect probe also
missed sessions from the known-good r16 runtime. Repeating r16 with an
interactive, session-1 scheduled probe showed active Realtek playback and AMD
microphone sessions. All audio-session claims below use that interactive probe.

The first r20 build did have a separate real defect: an intentionally missing
output caused 149 fallback warnings, reopening the default on nearly every
poll. The corrected r20b source keeps the working fallback open until SDL
enumerates the selected endpoint again. It also retains the initial playback
device until the first stream; the effect on idle host-device use is noted
below.

## r20b physical results

- The candidate started with explicit `Speaker (Realtek(R) Audio)` output and
  `Microphone Array (AMD Audio Device)` input on the isolated guest. PipeWire
  enumerated a virtio stereo sink and source.
- Before guest capture, the interactive probe showed the Realtek playback
  session and no AMD microphone session. During generated guest playback it
  showed **active Realtek playback** in five consecutive samples. A second
  playback after idle also showed five active samples.
- A bounded guest capture delivered **1,130,496 bytes** of mono 48 kHz signed
  16-bit samples; the interactive probe showed **active AMD microphone** in
  repeated samples. The samples were discarded. After capture, the microphone
  session became inactive while playback could remain active.
- Changing the output route to a nonexistent SDL name during playback caused
  **one** fallback warning. The Windows default Realtek playback session stayed
  active during the stream; subsequent polls did not repeat the warning.
  Restoring the default route allowed another active playback run.
- Changing the input route to a nonexistent SDL name during capture caused
  **one** recording fallback warning. The Windows default AMD microphone
  session remained active, and the guest received **1,417,216 bytes** before
  the bounded capture ended. Subsequent polls did not repeat the warning.
- On a separate boot with **Allow microphone access** disabled, QEMU used
  `in.voices=0`. A bounded guest recording attempt returned **zero bytes** and
  the interactive probe showed no recording session. Playback remained
  available. QEMU's `Could not create a backend for voice 'virtio-sound.in'`
  messages are expected for this explicit input-disabled mode.
- Each guest boot ended with `systemctl poweroff`. The original audio and
  desktop preferences were restored byte-for-byte from test backups; both
  route files were set to `default`. No candidate launcher or QEMU process
  remained. Only the canceled, incomplete `guest.next` staging directory from
  an earlier accidental latest-image download was removed.

## Remaining boundaries

There is only one active physical speaker and microphone on this laptop, so
switching between two real outputs or inputs, and physical unplug/reconnect,
remain untested. The interactive audio session proves the route is open, not
that a person heard smooth playback or intelligible microphone audio. The
corrected patch keeps the initial playback device open until the first stream;
idle device release before any playback is not established. This PR only makes
host Settings route changes live. It does not yet show Windows endpoints in
the guest PipeWire picker or handle host default changes through an endpoint
notification bridge. r20b is an engineering artifact, not a published or
pinned runtime.
